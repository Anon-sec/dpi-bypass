# Network settings
PROXY_IP = "127.0.0.1"
PROXY_PORT = 8080

# TLS split delays (safe to tweak)
TLS_DELAY_FIRST = 0.01
TLS_DELAY_SECOND = 0.01

# Socket timeout (used for clean shutdown)
SOCKET_TIMEOUT = 1.0

# DNS & logging controls
USE_DOH = True
LOG_ENABLED = True
LOG_DOH = True
